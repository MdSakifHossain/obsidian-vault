# Todo

- [ ] write some stuff about `systemd` for custom daemons
- [ ] try to make currently made `Obsidian Vault Observer` to be a custom command (`style-context.md`)
	- [ ] Add flags for live monitoring. Example `<command> -m` or `--monitor`.
	- [ ] Add flag for force push. Example `<command> -f` or `--force`.